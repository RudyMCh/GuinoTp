{% extends "base.html.twig" %}
{% block body %}

<div class="container">
    <h1>mes salons</h1>
    <ul>
        {% for lobby in myLobbies %}
        <li>
            <a href="{{ path('tchat', { "id": lobby.id }) }}">{{ lobby.name }} {{ lobby.city }}</a>
            
        </li>
        {% endfor %}
    </ul>
</div>
{% endblock %}