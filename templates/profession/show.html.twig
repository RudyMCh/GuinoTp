{% extends 'base.html.twig' %}

{% block title %}Profession{% endblock %}

{% block body %}
    <h1>Profession</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profession.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ profession.name }}</td>
            </tr>
        </tbody>
    </table>

    <div class="row">
        <div class="col">
            <a href="{{ path('profession_index') }}" style="padding-left: 12px">Retour à la liste</a>
        </div>
    </div>
    <div class="row">
        <div class="col"style="padding: 19px Opx 12px 0px>
            <a href="{{ path('profession_edit', {'id': profession.id}) }}" ">Modifier</a>
        </div>   
    </div>


    {{ include('profession/_delete_form.html.twig') }}
{% endblock %}
