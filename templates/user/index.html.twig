{% extends 'base.html.twig' %}

{% block title %}User index{% endblock %}

{% block body %}
    <h1>Liste des utilisateurs</h1>
    <div class="container-fluid">
            <div class="row">
                <div class="col-md-12">
                    <table class="table">
                        <caption>tableau status 0</caption>
                        <thead>
                            <tr>
                                <th>Nom</th>
                                <th>Prénom</th>
                                <th>Email</th>
                                <th>téléphone</th>
                                <th>Créé le</th>
                                <th>actions</th>
                            </tr>
                        </thead>
                        <tbody>
                                {% for user in users %}
                                {% if user.status ==  0 %}
                                    
                                <tr>
                                    <td>{{ user.lastname }}</td>
                                    <td>{{ user.firstname }}</td>
                                    <td>{{ user.email }}</td>
                                    <td>{{ user.phone }}</td>
                                    <td>{{ user.createdAt ? user.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                                    <td>
                                        <a href="{{ path('user_show', {'id': user.id}) }}">show</a>
                                        <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>
                                    </td>
                                </tr>

                                {% endif %}
                            {% endfor %}
                            
                        </tbody>
                    </table>
                    <table class="table">
                        <caption>tableau status 1</caption>
                        <thead>
                            <tr>
                                <th>Nom</th>
                                <th>Prénom</th>
                                <th>Email</th>
                                <th>téléphone</th>
                                <th>Créé le</th>
                                <th>actions</th>
                            </tr>
                        </thead>
                        <tbody>
                                {% for user in users %}
                                {% if user.status ==  1 %}
                                    
                                <tr>
                                    <td>{{ user.lastname }}</td>
                                    <td>{{ user.firstname }}</td>
                                    <td>{{ user.email }}</td>
                                    <td>{{ user.phone }}</td>
                                    <td>{{ user.createdAt ? user.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                                    <td>
                                        <a href="{{ path('user_show', {'id': user.id}) }}">show</a>
                                        <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>
                                    </td>
                                </tr>

                                {% endif %}
                            {% endfor %}
                            
                        </tbody>
                    </table>
                    <table class="table">
                        <caption>tableau status 2</caption>
                        <thead>
                            <tr>
                                <th>Nom</th>
                                <th>Prénom</th>
                                <th>Email</th>
                                <th>téléphone</th>
                                <th>Créé le</th>
                                <th>actions</th>
                            </tr>
                        </thead>
                        <tbody>
                                {% for user in users %}
                                {% if user.status == 2 %}
                                    
                                <tr>
                                    <td>{{ user.lastname }}</td>
                                    <td>{{ user.firstname }}</td>
                                    <td>{{ user.email }}</td>
                                    <td>{{ user.phone }}</td>
                                    <td>{{ user.createdAt ? user.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                                    <td>
                                        <a href="{{ path('user_show', {'id': user.id}) }}">show</a>
                                        <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>
                                    </td>
                                </tr>

                                {% endif %}
                            {% endfor %}
                            
                        </tbody>
                    </table>
                    <table class="table">
                        <caption>tableau status 3</caption>
                        <thead>
                            <tr>
                                <th>Nom</th>
                                <th>Prénom</th>
                                <th>Email</th>
                                <th>téléphone</th>
                                <th>Créé le</th>
                                <th>actions</th>
                            </tr>
                        </thead>
                        <tbody>
                                {% for user in users %}
                                {% if user.status == 3 %}
                                    
                                <tr>
                                    <td>{{ user.lastname }}</td>
                                    <td>{{ user.firstname }}</td>
                                    <td>{{ user.email }}</td>
                                    <td>{{ user.phone }}</td>
                                    <td>{{ user.createdAt ? user.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                                    <td>{{ user.password }}</td>
                                    <td>
                                        <a href="{{ path('user_show', {'id': user.id}) }}">show</a>
                                        <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>
                                    </td>
                                </tr>

                                {% endif %}
                            {% endfor %}
                            
                        </tbody>
                    </table>
                    <table class="table">
                        <caption>tableau status 4</caption>
                        <thead>
                            <tr>
                                <th>Nom</th>
                                <th>Prénom</th>
                                <th>Email</th>
                                <th>téléphone</th>
                                <th>Créé le</th>
                                <th>actions</th>
                            </tr>
                        </thead>
                        <tbody>
                                {% for user in users %}
                                {% if user.status == 4 %}
                                    
                                <tr>
                                    <td>{{ user.lastname }}</td>
                                    <td>{{ user.firstname }}</td>
                                    <td>{{ user.email }}</td>
                                    <td>{{ user.phone }}</td>
                                    <td>{{ user.createdAt ? user.createdAt|date('Y-m-d H:i:s') : '' }}</td>
                                    <td>
                                        <a href="{{ path('user_show', {'id': user.id}) }}">show</a>
                                        <a href="{{ path('user_edit', {'id': user.id}) }}">edit</a>
                                    </td>
                                </tr>

                                {% endif %}
                            {% endfor %}
                            
                        </tbody>
                    </table>
                    
                </div>
            </div>
        </div>

    <a href="{{ path('user_new') }}">Create new</a>
{% endblock %}
